// Zig HTTP Server — Client-side helpers
// Minimal JS for htmx enhancements (Step 16 will expand this)

document.addEventListener("DOMContentLoaded", function () {
    console.log("[zig-server] static assets loaded");

    // future: htmx event hooks, chart rendering, etc.
});
